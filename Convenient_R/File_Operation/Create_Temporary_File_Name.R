tempfile(pattern = 'tmp', tmpdir = tempdir(), fileext = '')
# pattern: a character vector suggesting the initial part of the name.
# tmpdir: a character vector suggesting the directory name.
# fileext: a character vector suggesting the file extension.
